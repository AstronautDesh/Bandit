.card-details {
  display: flex;
  flex-direction: column;
}

.card-details input {
  font-family: 'NovaSquare';
  font-size: 1rem;
  border: none; /* Remove default input border */
  border-bottom: 2px solid #ccc; /* Add a custom bottom border */
  padding: 0.5rem 0; /* Add some padding for spacing */
  transition: border-color 0.3s, transform 0.3s; /* Smooth transition */

  /* Placeholder styling */
  &::placeholder {
    color: #999;
    font-style: italic;
  }

  &:focus,
  &:hover {
    border-color: #007bff; /* Highlight border color on focus/hover */
    transform: translateY(-5px); /* Move input field up slightly */
  }
}



/* Style the info icon (you can adjust this as needed) */
.info-icon {
  font-size: 1.2rem;
  color: #999;
}

















.card-details input {
  font-family: 'NovaSquare';
  font-size: 1rem;
  border: none;
  border-bottom: 2px solid #ccc;
  padding: 0.5rem 0;
  transition: border-color 0.3s, transform 0.3s;

  &::placeholder {
    color: #999;
  }

  &:focus,
  &:hover {
    border-color: #007bff;
    transform: translateY(-5px);
  }
}

.form-row {
  display: flex;
  justify-content: space-around;
  gap: 0.7rem;
  margin-top: 0.5rem;
}

.info-icon {
  font-size: 1.2rem;
  color: #999;
}

.card-number {
   display: flex;
  margin-bottom: 2rem;
  background-color: wheat;
  border: 2px solid red;
}

.card::before {
  content: '';
  position: absolute;
  top: 0;
  right: 1rem;
  width: 100%;
  height: 100%;
  background-size: cover;
  background-repeat: no-repeat;
  background-position: center;
}
form {
  display: flex;
  flex-direction: column;
}
input {
  background-color: transparent;
  border: none;
  border-bottom: 1px solid #333;
  color: #fff;
  margin-bottom: 1rem;
  }
  button {
    background-color: #000;
    color: #fff;
    border: none;
    padding: 1rem;
    cursor: pointer;
    margin-top: 1rem;
  }

